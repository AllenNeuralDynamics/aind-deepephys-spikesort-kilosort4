{
  "parameters": [
    {
      "default_value": "true",
      "description": "Whether to raise an error if spike sorting fails.",
      "name": "raise if fails",
      "type": "list",
      "value_options": [
        "true",
        "false"
      ],
      "value_type": "string"
    },
    {
      "default_value": "false",
      "description": "Whether to skip Kilosort motion correction.",
      "name": "skip motion correction",
      "type": "list",
      "value_options": [
        "true",
        "false"
      ],
      "value_type": "string"
    },
    {
      "default_value": "64",
      "description": "Minimum number of channels required to enable Kilosort motion correction.",
      "name": "min channels for drift",
      "type": "text",
      "value_type": "string"
    },
    {
      "default_value": "false",
      "description": "Force PyTorch to release cached memory between memory-intensive operations.",
      "name": "clear cache",
      "type": "list",
      "value_options": [
        "false",
        "true"
      ],
      "value_type": "string"
    },
    {
      "description": "Complete Kilosort wrapper configuration as a JSON string. When provided, it overrides the other sorter arguments.",
      "help_text": "Use a complete payload containing `job_kwargs`, `sorter`, and wrapper controls. Partial sorter dictionaries are not accepted by the capsule.",
      "name": "Kilosort parameter JSON",
      "param_name": "params",
      "required": false,
      "type": "text",
      "value_type": "string"
    }
  ]
}
